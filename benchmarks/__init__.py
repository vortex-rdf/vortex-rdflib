"""CodSpeed benchmark suite (see ``benchmarks/README.md``).

A package (rather than a bare directory) so pytest puts the repository root
on ``sys.path`` and the suite can reuse the dataset and query generators in
``bench/``.
"""
